osx_image: xcode7.3
language: objective-c
before_install:
  - bundle install
install:
  - cd Example ; bundle exec pod install; cd ..
xcode_workspace: Example/ElloCerts.xcworkspace
xcode_scheme: ElloCerts-Example
xcode_sdk: iphonesimulator
